# ==============================================
# .gitignore - Files to exclude from Git
# ==============================================
# This file tells Git which files/folders to ignore.
# Ignored files won't be tracked or committed to the repository.

# ----------------------------------------------
# SECURITY: Firebase credentials - NEVER commit!
# ----------------------------------------------
# This file contains private keys for Firebase authentication.
# If committed, attackers could access your Firebase database.
# Each developer should get their own copy from Firebase Console.
serviceAccountKey.json

# ----------------------------------------------
# Maven build output folder
# ----------------------------------------------
# These are generated by 'mvn compile' and 'mvn package'.
# They can be regenerated anytime, so no need to track them.
target/
*.class

# ----------------------------------------------
# IDE configuration files
# ----------------------------------------------
# These are personal IDE settings that vary per developer.
# Each developer has different IDE preferences.
.idea/
*.iml
.vscode/

# ----------------------------------------------
# OS-generated files
# ----------------------------------------------
# These are hidden system files created by the operating system.
# macOS creates .DS_Store, Windows creates Thumbs.db
.DS_Store
Thumbs.db
nul

# ----------------------------------------------
# Claude Code workspace
# ----------------------------------------------
# AI assistant configuration and cache files.
# These are personal to each developer's Claude setup.
.claude/
